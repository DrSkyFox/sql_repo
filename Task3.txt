пользователь не может дважды лайкнуть одну и ту же сущность;
пользователь имеет право отозвать лайк;
необходимо иметь возможность считать число полученных сущностью лайков и выводить список пользователей, поставивших лайки;
в будущем могут появиться новые виды сущностей, которые можно лайкать.


 Можно сделать процудуру (метод), которая будет выполняться при нажатии на кнопочку like. 
Процедура будет проверять, поставлен ли like этим пользователем на данном комментарии или фото
Если да, то записиь переключить в состоянии enabled = 0. Если нет еще, то создать запись в таблице о том, что пользователь поставил like.


select * from photo_id
where photo_id = :id;

select count(*) from photo_id
group by photo_id;

Второй вариант создать дополнительное поле (c_liked) в таблице photo со значением кол-ва лайков. И использовать триггер при установке like изменять поле c_liked, как только у нас добавляется/отключается запись в таблице photo_likes для photo_id. 
